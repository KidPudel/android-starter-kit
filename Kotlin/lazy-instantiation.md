# Lazy instatiation

lazy instantiation is the tacticof delaying the creationof an object, the calculation of the value, or some other expensive process untill the first time is needed
actually create an instance on the first access
